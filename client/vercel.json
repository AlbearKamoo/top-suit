{
  "buildCommand": "cd ../shared && npm install && npm run build && cd ../client && npm install && vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
} 